import { CustomTableFilterPipe } from './custom-table-filter.pipe';

describe('CustomTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
